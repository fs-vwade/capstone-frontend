import React from "react";
// import ProgressBar from "./ProgressBar"; - not sure if this module? or just folder path

const App = () => {
  return (
    <div>
      <ProgressBar complete={80} />
    </div>
  );
};

export default App;
